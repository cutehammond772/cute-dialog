export { usePatch } from "@patcher/hooks";

export { DialogProvider, DialogArea } from "@provider/components";

export { useDialogProvider } from "@provider/hooks";

export { useDialog } from "@resolver/hooks";

export type { Patch } from "@/patch/patcher";

export type { HandleEventHandler, PatchEventCallback } from "@/patch/event";

export type { DialogElement, DialogProfile, DialogTemplate } from "@/common";
